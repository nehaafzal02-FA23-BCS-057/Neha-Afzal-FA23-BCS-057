name: flutter_sqlite_quiz2
description: A Flutter CRUD app using SQLite and Image Picker
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0
  path_provider: ^2.1.2
  image_picker: ^1.1.0
  path: ^1.9.0

flutter:
  uses-material-design: true
